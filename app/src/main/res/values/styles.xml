<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomPlayButton" parent="ExoMediaButton.Pause">
        <item name="android:src">@drawable/ic_play_circle_outline_white_60dp</item>
    </style>

    <style name="CustomPauseButton" parent="ExoMediaButton.Pause">
        <item name="android:src">@drawable/ic_pause_circle_outline_white_60dp</item>
    </style>

</resources>
